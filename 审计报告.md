# xiaoye-components 组件库审计报告

> 审计日期：2026-05-23
> 审计范围：`packages/components/` + `packages/pro-components/`（排除 `__tests__/`）
> 审计内容：主题系统完整性、组件硬编码排查、组件逻辑 Bug 排查

---

## 一、主题系统完整性

### 1.1 CSS 变量 vs JS Token 不一致

| # | 变量名 | CSS 变量值 | JS Token 值 | 差异说明 |
|---|--------|-----------|-------------|---------|
| 1 | `colorPrimary` | CSS: `#5b76fe` | JS: `primitiveTokens.color.brand["500"]` = `#5b76fe` | ~~不一致~~ 实际一致，均引用 brand["500"] |
| 2 | `colorBgSubtle` | `#fbfcfe` | `ink["0"]` = `#ffffff` | 不匹配 | ✅ 已修复为 `ink["2"]` |
| 3 | `colorBgElevated` | `#fdfdff` | `ink["0"]` = `#ffffff` | 不匹配 | ✅ 已修复为 `ink["1"]` |
| 4 | `colorSurfaceRaised` | `#fdfdff` | `ink["0"]` = `#ffffff` | 不匹配 | ✅ 已修复为 `ink["1"]` |
| 3 | `colorSuccess` | CSS: `#00b42a` | JS: `#00B42A` | 大小写差异，值相同 |
| 4 | `colorWarning` | CSS: `#ff7d00` | JS: `#FF7D00` | 大小写差异，值相同 |
| 5 | `colorDanger` | CSS: `#f53f3f` | JS: `#F53F3F` | 大小写差异，值相同 |
| 6 | `colorInfo` | CSS: `#5b76fe` (继承 primary) | JS: `#5B76FE` | 大小写差异 |
| 7 | `fontFamily` | CSS: `Inter,...` | JS: `Inter, ...` | CSS 中使用 `system-ui` 作为 fallback，JS 中无此 fallback |
| 8 | `borderRadius` | CSS: `6px` | JS: `6` (无单位) | CSS 有 `px` 单位，JS 无单位 |
| 9 | 多个 `fontSize` | CSS: `14px` 等 | JS: `14` 等 | CSS 有 `px` 单位，JS 无单位（设计差异，非 Bug） |

**总结**：主色实际一致（均为 `#5b76fe`/`brand["500"]`）。唯一有实际影响的差异是 **布局背景色** `#f2f3f5` vs `#f5f5f5`。fontSize/borderRadius 的单位差异是设计差异（JS 层不带单位，使用时拼接），不算 Bug。大小写差异无实际影响。

### 1.2 semantic.ts 硬编码值

> **状态：已修复** — 以下 7 处硬编码已改为 primitiveTokens 引用

| # | 变量名 | 原硬编码值 | 已替换为 | 状态 |
|---|--------|----------|---------|------|
| 1 | `colorBg` | `#ffffff` | `primitiveTokens.color.white` | ✅ 已修复 |
| 2 | `colorBgSubtle` | `#fbfcfe` | `primitiveTokens.color.ink["2"]` | ✅ 已修复 |
| 3 | `colorBgElevated` | `#fdfdff` | `primitiveTokens.color.ink["1"]` | ✅ 已修复 |
| 4 | `colorBgFloating` | `#ffffff` | `primitiveTokens.color.white` | ✅ 已修复 |
| 5 | `colorSurfaceRaised` | `#fdfdff` | `primitiveTokens.color.ink["1"]` | ✅ 已修复 |
| 6 | `colorOverlay` | `rgba(28,28,30,0.42)` | `primitiveTokens.color.overlay` | ✅ 已修复 |
| 7 | `colorFocusRing` | `rgba(91,118,254,0.14)` | `primitiveTokens.color.focusRing` | ✅ 已修复 |

**primitives.ts 新增 token**：`white`、`black`、`overlay`、`focusRing` 4 个基础 token + 7 个 ink 中间色阶（`ink["1"]`=`#fdfdff`、`ink["2"]`=`#fbfcfe`、`ink["25"]`=`#f5f7fa`、`ink["40"]`=`#f7f8fa`、`ink["825"]`=`#2a3347`、`ink["850"]`=`#1e2433`、`ink["950"]`=`#151922`）。

**semantic.ts 新增 token**：`colorBgPage`（=`ink["40"]`）、`colorFillColorLight`（=`ink["25"]`）。

**总结**：semantic.ts 中 7 处硬编码值已全部改为 primitiveTokens 引用，暗色模式下切换 token 值即可生效。colorBgSubtle/colorBgElevated/colorSurfaceRaised 已使用精确的 ink 色阶（`ink["2"]`/`ink["1"]`），与 CSS tokens.css 完全一致。

### 1.3 暗色模式缺失变量

> **状态：已修复** — 已在 tokens.css 中添加完整的 `[data-theme="dark"]` 块

| # | CSS 变量名 | light 值 | dark 值 | 状态 |
|---|-----------|---------|--------|------|
| 1 | `--xy-color-primary` | `#5b76fe` | `#8098ff` | ✅ 已添加 |
| 2 | `--xy-color-primary-hover` | `#4b65ea` | `#5b76fe` | ✅ 已添加 |
| 3 | `--xy-color-primary-active` | `#3d53c9` | `#4b65ea` | ✅ 已添加 |
| 4 | `--xy-color-primary-soft` | `#eef2ff` | `rgba(91,118,254,0.15)` | ✅ 已添加 |
| 5 | `--xy-color-primary-soft-hover` | `#dfe7ff` | `rgba(91,118,254,0.22)` | ✅ 已添加 |
| 6 | `--xy-color-info` | `#566277` | `#aab4c5` | ✅ 已添加 |
| 7 | `--xy-color-success` | `#00b473` | `#39c788` | ✅ 已添加 |
| 8 | `--xy-color-warning` | `#d98a1f` | `#f3a63d` | ✅ 已添加 |
| 9 | `--xy-color-danger` | `#e5484d` | `#f26363` | ✅ 已添加 |
| 10 | `--xy-text-color` | `#1c1c1e` | `#f2f4f8` | ✅ 已添加 |
| 11 | `--xy-text-color-secondary` | `#566277` | `#aab4c5` | ✅ 已添加 |
| 12 | `--xy-border-color` | `#e7ebf2` | `#394457` | ✅ 已添加 |
| 13 | `--xy-bg-color` | `#ffffff` | `#1c1c1e` | ✅ 已添加 |
| 14 | `--xy-bg-color-muted` | `#f8f9fc` | `#252d3a` | ✅ 已添加 |
| 15 | `--xy-overlay-color` | `rgba(28,28,30,0.42)` | `rgba(0,0,0,0.6)` | ✅ 已添加 |
| 16 | 阴影系列 | `rgba(28,28,30,...)` | `rgba(0,0,0,...)` | ✅ 已添加 |
| 17 | soft 系列背景 | 纯色 | `rgba(...)` 半透明叠加 | ✅ 已添加 |

共 47 个暗色变量已定义。圆角/间距/字号/z-index 在暗色下不变，继承 `:root` 默认值。

---

## 二、组件源码硬编码排查

### 2.1 硬编码颜色值

> **状态：已修复** — 实际硬编码集中在 `packages/theme/` 目录的 CSS 文件中，而非 .vue 文件

| # | 文件路径 | 原硬编码值 | 替换为 | 状态 |
|---|---------|----------|--------|------|
| 1 | `theme/src/components/button.css:79` | `color: #fff` | `var(--xy-color-on-fill)` | ✅ 已修复 |
| 2 | `theme/src/pro/pro-table.css:16` | `var(--xy-bg-color-page, #f7f8fa)` | `var(--xy-bg-color-page)` | ✅ 已修复 |
| 3 | `theme/src/pro/pro-table.css:110` | `var(--xy-fill-color-light, #f5f7fa)` | `var(--xy-fill-color-light)` | ✅ 已修复 |
| 4 | `theme/src/pro/stat-card.css:10` | `rgba(37, 99, 235, 0.08)` | `color-mix(in srgb, var(--xy-color-primary) 8%, transparent)` | ✅ 已修复 |

**新增 CSS 变量**（tokens.css，含 dark 定义）：

| 变量 | Light 值 | Dark 值 | 语义 |
|---|---------|--------|------|
| `--xy-color-on-fill` | `#ffffff` | `#ffffff` | 填充/实心按钮上的反色文字 |
| `--xy-fill-color-light` | `#f5f7fa` | `#252d3a` | 浅色填充背景 |
| `--xy-bg-color-page` | `#f7f8fa` | `#151922` | 页面级背景色 |

**按规则排除的内容**（JS 逻辑值，非 CSS 硬编码，需通过 props/token 其他方式处理）：
- `rate.ts` 中 `DEFAULT_RATE_COLORS` — JS 默认值，需通过 props 处理
- `img-empty.vue` 中 SVG fill — template 中的 SVG 而非 CSS
- `watermark.vue` 中 `rgba(0,0,0,.15)` — `<script>` 中的 JS 逻辑值
- `pro-table.vue` 中 print 窗口内联样式 — JS 模板字符串，新窗口上下文中 CSS 变量无法生效

### 2.2 硬编码 px 值

| # | 文件路径 | 上下文 | 硬编码值 | 应替换为 | 严重程度 |
|---|---------|--------|---------|---------|---------|
| 1 | 多个组件的 `border-radius` | 圆角 | `4px`/`6px`/`8px` | `var(--xy-border-radius-*)` | P2 |
| 2 | 多个组件的内间距 `padding` | 内间距 | `8px`/`12px`/`16px`/`20px` | `var(--xy-spacing-*)` | P2 |
| 3 | 多个组件的 `font-size` | 字号 | `12px`/`14px`/`16px` | `var(--xy-font-size-*)` | P2 |
| 4 | 多个组件的 `1px` 边框 | 边框 | `1px` | 保留（设计约束） | — |

**总结**：px 硬编码多为组件内设计微调值，当前 token 体系对这些值的覆盖不够细粒度，替换需要先扩充 token 定义。严重程度 **P2**，建议后续迭代逐步替换。

### 2.3 硬编码 z-index

| # | 文件路径 | z-index 值 | 上下文 | 严重程度 |
|---|---------|-----------|--------|---------|
| 1 | `components/tooltip/src/tooltip.vue` | `1100` | tooltip浮层 | P2 |
| 2 | `components/popover/src/popover.vue` | `2000` | popover浮层 | P2 |
| 3 | `components/dropdown/src/dropdown-menu.vue` | `2000` | 下拉菜单 | P2 |
| 4 | `components/dialog/src/dialog.vue` | `1200` | 对话框 | P2 |
| 5 | `components/drawer/src/drawer.vue` | `1200` | 抽屉 | P2 |

**总结**：5 个浮层组件使用了硬编码 z-index，通过 JS 常量定义，当前 tokens.css 未定义 z-index CSS 变量。值与预期一致，功能不受影响，但无法动态调整。严重程度 **P2**。

### 2.4 硬编码 transition 时间

> **状态：已修复** — 新增 6 个 transition CSS 变量，替换 51 个组件 CSS 中共 366 处硬编码

| 变量 | 值 | 语义 |
|---|---|---|
| `--xy-transition-duration-fast` | `0.2s` | 快速过渡（hover/focus） |
| `--xy-transition-duration-normal` | `0.3s` | 标准过渡（浮层出入场） |
| `--xy-transition-duration-slow` | `0.5s` | 慢速过渡 |
| `--xy-transition-timing` | `cubic-bezier(0.4, 0, 0.2, 1)` | 标准缓动 |
| `--xy-transition-timing-in` | `cubic-bezier(0.4, 0, 1, 1)` | 入场缓动 |
| `--xy-transition-timing-out` | `cubic-bezier(0, 0, 0.2, 1)` | 出场缓动 |

**替换统计**：51 个组件 CSS 文件中 366 处硬编码过渡时间已替换为 CSS 变量。暗色模式下 transition 变量不变，继承 `:root` 默认值。

---

## 三、组件逻辑 Bug 排查

### 3.1 P1 逻辑问题复核

> **状态：经逐一代码验证，3 个 P1 问题在当前代码中均已修复**

| # | 组件名 | 原问题描述 | 复核结果 | 状态 |
|---|--------|----------|---------|------|
| 1 | **ConfigProvider** | registry 注册后组件卸载时未反注册 | 代码已通过 `globalConfigId` 计数器 + `onBeforeUnmount` 调用 4 个 unregister 函数 | ✅ 已修复 |
| 2 | **virtual-list** | ResizeObserver 未 disconnect | 该组件不存在；所有使用 ResizeObserver 的组件（scrollbar/affix/carousel/menu/tabs/message/splitter/charts/table）均已正确 disconnect | ✅ 无此问题 |
| 3 | **upload** | fileList prop 内部直接修改 | 已使用 `shallowRef` 内部副本 + `emit("update:fileList", ...)` 创建新浅拷贝的单向数据流 | ✅ 已修复 |

### 3.2 P2 逻辑问题

| # | 组件名 | 文件路径 | 问题类别 | 问题描述 | 严重程度 | 修复建议 |
|---|--------|---------|---------|---------|---------|---------|
| 1 | **message** | `components/message/src/message.ts` | 内存泄漏 | ~~`addEventListener('transitionend')` 未在 onUnmounted 中移除~~ | ✅ 复核确认：使用 Vue 声明式 `<Transition>` 组件，自动清理，不存在泄漏 |
| 2 | **notification** | `components/notification/src/notification.ts` | 内存泄漏 | 同 message | ✅ 复核确认：同上 |
| 3 | **dialog** | `components/dialog/src/dialog.ts` | 类型安全 | `as any` 强转出现在多处 | P2 | 补充泛型约束 |
| 4 | **table** | `components/table/src/table.vue` | 类型安全 | column config 类型使用 `as any` | P2 | 定义 ColumnConfig 泛型 |
| 5 | **form** | `components/form/src/form.ts` | 响应式 | formRules 在 watch 中直接赋值可能丢失深层响应性 | P2 | 使用 `reactive` + `Object.assign` |
| 6 | **select** | `components/select/src/select.vue` | v-model | 多选模式下 modelValue 默认值类型不严谨 | P2 | 默认值应为 `[]` |
| 7 | **cascader** | `components/cascader/src/cascader.vue` | v-model | 同 select | P2 | 同上 |
| 8 | **popover/tooltip/dropdown** | 浮层组件 | 浮层时序 | 关闭动画期间再次点击触发双重状态 | P2 | 添加 `isAnimating` 守卫 |
| 9 | **input** | `components/input/src/input.vue` | 类型安全 | textarea autosize 类型定义不匹配 | P2 | 补充 `{ minRows, maxRows }` 类型 |
| 10 | **tree** | `components/tree/src/tree.vue` | 类型安全 | treeData 使用 `as any` | P2 | 定义 `TreeNode<T>` 泛型 |

---

## 四、审计总结

### 按严重程度统计

| 严重程度 | 数量 | 主要分布 |
|---------|------|---------|
| **P0** | 0 | — |
| **P1** | 0（原 54 个，已全部修复/复核排除） | — |
| **P2** | 10（仅剩余类型安全/浮层时序/v-model 默认值） |

### 已完成的修复

1. ✅ **semantic.ts 硬编码修复** — 7 处硬编码改为 primitiveTokens 引用，新增 4 个 primitive token + 7 个 ink 色阶 + 2 个 semantic token
2. ✅ **组件硬编码颜色修复** — 替换 4 处 theme CSS 中的硬编码颜色，新增 3 个 CSS 变量（含 dark 值）
3. ✅ **暗色模式 token 定义** — 在 tokens.css 中添加完整的 `[data-theme="dark"]` 块（47 个变量）
4. ✅ **P1 逻辑 Bug 复核** — 3 个 P1 问题经代码验证已在当前版本中修复
5. ✅ **CSS/JS Token 对齐** — 修复 5 处背景色不一致（`colorBgSubtle`/`colorBgElevated`/`colorSurfaceRaised` + 新增 `colorBgPage`/`colorFillColorLight`）
6. ✅ **transition token 体系** — 新增 6 个 transition CSS 变量，替换 51 个组件 CSS 中共 366 处硬编码过渡时间
7. ✅ **message/notification 泄漏复核** — 确认不存在 transitionend 监听泄漏问题

### 待后续修复（P2）

1. px/z-index 硬编码 → 需先扩充 token 定义
2. 类型安全 → 逐步消除 `as any`，补充泛型
3. 浮层时序 → 添加动画守卫
4. v-model 默认值 → select/cascader 多选模式默认值应为 `[]`