import type { AsyncStateContainerProps } from "@xiaoye/pro-components";

const props: AsyncStateContainerProps = {
  loading: false,
  error: null,
  empty: true,
  emptyTitle: "暂无结果"
};

void props;
