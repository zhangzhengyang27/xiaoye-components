import type { FilterPanelProps } from "@xiaoye/pro-components";

const props: FilterPanelProps = {
  title: "筛选条件",
  description: "支持折叠",
  defaultCollapsed: false,
  collapsible: true
};

void props;
